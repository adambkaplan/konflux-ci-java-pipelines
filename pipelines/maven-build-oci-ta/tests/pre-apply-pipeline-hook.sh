#!/bin/bash

# This script is called before applying the pipeline to set up required resources.
# The CI environment provides trusted-ca and kind-registry access.

echo "Pre-requirements setup complete for maven-build-oci-ta pipeline"
